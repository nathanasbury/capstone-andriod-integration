package com.example.guardianhealth.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary Palette (Teal / Cyan) ────────────────────────────
val GreenPrimary = Color(0xFF00897B)        // Teal 600
val GreenLight = Color(0xFF4DB6AC)          // Teal 300
val GreenSoft = Color(0xFFB2DFDB)           // Teal 100
val GreenContainer = Color(0xFFE0F2F1)      // Teal 50
val MintAccent = Color(0xFF1DE9B6)          // Teal accent bright

// ── Background & Surface ─────────────────────────────────────
val Background = Color(0xFFF9FAFB)
val Surface = Color(0xFFFFFFFF)
val SurfaceDim = Color(0xFFF3F4F6)

// ── Text ─────────────────────────────────────────────────────
val TextPrimary = Color(0xFF111827)
val TextSecondary = Color(0xFF9CA3AF)
val TextOnPrimary = Color(0xFFFFFFFF)

// ── Vital Accent Colors ──────────────────────────────────────
val HeartRed = Color(0xFFE53E3E)
val HeartRedBg = Color(0xFFFEF2F2)
val OxygenBlue = Color(0xFF3B82F6)
val OxygenBlueBg = Color(0xFFEFF6FF)
val StepsAmber = Color(0xFFF59E0B)
val StepsAmberBg = Color(0xFFFFFBEB)

// ── Alert ────────────────────────────────────────────────────
val AlertRed = Color(0xFFDC2626)
val AlertRedBg = Color(0xFFFEE2E2)

// ── Status ───────────────────────────────────────────────────
val ConnectedGreen = Color(0xFF10B981)
val DisconnectedGray = Color(0xFFD1D5DB)

// ── Borders & Dividers ───────────────────────────────────────
val BorderLight = Color(0xFFE5E7EB)
val CardBorder = Color(0xFFE5E7EB)
